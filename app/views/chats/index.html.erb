<p style="color: green"><%= notice %></p>

<% content_for :title, "Chats" %>

<h1>Chats</h1>

<div id="chats">
  <% @chats.each do |chat| %>
    <p>
      <%= link_to name_for(chat), chat %>
    </p>
  <% end %>
</div>

<%= link_to "New chat", new_chat_path %>
